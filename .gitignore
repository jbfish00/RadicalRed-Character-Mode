# ROM/patch binaries — never commit copyrighted material
rom/*.gba
rom/*.zip
*.gba
*.ips
*.ups
*.bps
build/
__pycache__/
*.pyc

# armips is a third-party source clone (github.com/Kingcom/armips), not our code
tools/armips/
# local compiled/copied tool binaries — regenerate via build steps, not committed
tools/bin/

# Ghidra install + analysis project — huge, regenerable, not our code
tools/ghidra/
ghidra_project/

# Complete FireRed Upgrade clone — third-party donor data (Radical Red's own
# upstream engine), regenerable via git clone, not our code
tools/cfru_donor/

docs/strings_dump.txt
docs/ghidra_analysis.log
docs/all_strings_dump.txt
docs/all_strings_dump.stderr

# emulator save files (created by mgba test runs)
*.sav
# generated by tools/tests/shim_unit_test.py on each run
tools/tests/shim_test.gdb

# Local Claude Code handoff notes (kept out of the public repo)
CLAUDE.md
