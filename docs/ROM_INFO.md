# Source ROM

- File: `radicalred 4.1.gba`
- Size: 33,554,432 bytes (32 MiB)
- SHA1: `964f951a0fdaf209e4ea1344883ef0d557bb3a80` (see rom.sha1)
- Header: title `POKEMON FIRE`, game code `BPRE`, maker `01`, version byte `0` — confirms FireRed (English, rev 0) base, consistent with Radical Red being a Complete FireRed Upgrade (CFRU)-derived hack (same base as Unbound).
- Provenance: extracted from `radicalred 4.1.zip` (workspace root), which contained a single file with no accompanying readme/watermark. No published SHA1 exists for v4.1 anywhere the initial research turned up — this project computes and owns the canonical pin above.
- All addresses/offsets recorded anywhere in this project are pinned to this exact SHA1. A differently-repackaged "same version" file must be re-verified before trusting any note here against it.
